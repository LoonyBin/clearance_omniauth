<% if signed_in? %>
  <%= current_user.email %>
  <%= button_to "Logout", sign_out_path, :method => :delete %>
<% else %>
  <%= button_to "Register", sign_up_path %>
  <%= button_to "Login", sign_in_path %>
<% end %>
